apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-service
  labels:
    app: user-service
    release: prometheus
spec:
  selector:
    matchLabels:
      app: user-service
  namespaceSelector:
    matchNames:
      - auth-platform
  endpoints:
    - port: http
      path: /api/actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s

name: remediator
description: AI agent that executes remediation actions for vulnerabilities
type: container
team: platform

container:
  ecr_pattern: "shara-remediator"
  dockerfile: ./Dockerfile
  base_image: python:3.12-slim
  port: 8081
  health_check: /ping

language: python
framework: fastapi
runtime: python3.12

dependencies:
  - path: ./requirements.txt
    type: pip
    description: Python package dependencies

vulnerability_remediation:
  base_image:
    file: ./Dockerfile
    pattern: "FROM python:3.12-slim"
    line_prefix: "FROM"
  packages:
    - file: ./requirements.txt
      type: pip
      format: "{package}=={version}"

tags:
  - ai-agent
  - security
  - bedrock-agentcore
  - mcp-tools

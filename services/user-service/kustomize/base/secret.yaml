apiVersion: v1
kind: Secret
metadata:
  name: user-service-secret
type: Opaque
stringData:
  # Database credentials
  # These should be replaced with actual values from Secrets Manager
  # Secret ARN: auth-platform-production/aurora-credentials
  DB_HOST: "${AURORA_CLUSTER_ENDPOINT}"
  DB_USERNAME: "admin"
  DB_PASSWORD: "${DB_PASSWORD}"

  # Cognito User Pool ID
  # Get from: terraform output cognito_user_pool_id
  COGNITO_USER_POOL_ID: "${COGNITO_USER_POOL_ID}"

  # Internal API Key (for service-to-service communication)
  INTERNAL_API_KEY: "${INTERNAL_API_KEY}"
---
# Production: Use ExternalSecret with AWS Secrets Manager
# Install External Secrets Operator first:
# helm install external-secrets external-secrets/external-secrets -n external-secrets --create-namespace
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: user-service-external-secret
#   namespace: auth-platform
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: user-service-secret
#     creationPolicy: Owner
#   data:
#     - secretKey: DB_HOST
#       remoteRef:
#         key: auth-platform-production/aurora-credentials
#         property: host
#     - secretKey: DB_USERNAME
#       remoteRef:
#         key: auth-platform-production/aurora-credentials
#         property: username
#     - secretKey: DB_PASSWORD
#       remoteRef:
#         key: auth-platform-production/aurora-credentials
#         property: password
#     - secretKey: COGNITO_USER_POOL_ID
#       remoteRef:
#         key: auth-platform-production/cognito
#         property: user_pool_id

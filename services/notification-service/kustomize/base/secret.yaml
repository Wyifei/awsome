apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
type: Opaque
stringData:
  # SES Configuration
  # Get SES_FROM_ADDRESS from: terraform output (or terraform.tfvars ses_email_address)
  SES_FROM_ADDRESS: "${SES_FROM_ADDRESS}"
---
# Production: Use ExternalSecret with AWS Secrets Manager
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: notification-service-external-secret
#   namespace: auth-platform
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: notification-service-secret
#     creationPolicy: Owner
#   data:
#     - secretKey: SES_FROM_ADDRESS
#       remoteRef:
#         key: auth-platform-production/ses
#         property: from_address

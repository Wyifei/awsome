apiVersion: v1
kind: ServiceAccount
metadata:
  name: profile-service-sa
  annotations:
    # Use shared app-service-role from infrastructure
    # Role ARN format: arn:aws:iam::${AWS_ACCOUNT_ID}:role/auth-platform-production-app-service-role
    eks.amazonaws.com/role-arn: ${APP_SERVICE_ROLE_ARN}
